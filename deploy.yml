---
- name: Deploy artifact to localhost
  hosts: localhost
  connection: local

  tasks:
    - name: Copy the artifact to target location
      copy:
        src: "/var/lib/jenkins/workspace/devops8/target/mvn-app-1.0-SNAPSHOT.jar"
        dest: "/tmp/mvn-app.jar"
        remote_src: yes
